# 画出事件循环阶段

## 从任务来源开始

不要背诵完整阶段列表。先判断任务来自定时器、I/O 还是立即回调。

## 关系骨架

1. 定时器到期后进入 timers 阶段。
2. 一部分 I/O 回调在 poll 阶段处理。
3. `setImmediate` 回调在 check 阶段处理。

## 用新例子检验

给出一个包含文件读取和 `setImmediate` 的例子，并解释你还缺少哪条上下文才能预测完整顺序。
