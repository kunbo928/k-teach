---
"k-teach": major
---

Add resumable `generate` / `context` / `inspect` workflows with Context Packets, Semantic Plans, content-addressed caching, and staged artifact promotion. Require Presentation Briefs for PPT rendering and drop the legacy V1 publication contract migrations.
